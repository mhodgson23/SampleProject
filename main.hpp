/**
* Analysis of Wind Turbine Power Output Versus Consumption based on randomized phenomena.
* @author Michael Hodgson
* @date 06.14.2020
* @version 0.0.1
*
*/

#include <cstdlib>

/**
* Primary Execution Path for Wind Turbine testing.
*
* @param argc Count of arguments into main function.
* @param argv Arguments into main function.
*
* @return 
*
* Calling main with no arguments will run the optimizer with default settings.
* Calling main with the first argument set to an integer 0 through 10 will call unit test 0 through 10.
* Calling main with any other set of arguments will operate with the default settings.
*/
int main(int argc, char *argv[]);